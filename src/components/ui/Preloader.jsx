/* eslint-disable import/extensions */
import React from 'react';
import CircularProgress from './CircularProgress';

const Preloader = () => (
    <div className="preloader">
        <h4>FabekDesignsIntl</h4>
        <br/>
        <CircularProgress />
    </div>
);

export default Preloader;
